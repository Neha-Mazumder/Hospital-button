# Hospital Management System - আর্কিটেকচার

## 🏗️ সিস্টেম আর্কিটেকচার:

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND (Vite/React)                    │
│                     http://localhost:5173                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  LoginPage.tsx      RegisterPage.tsx      HomePage.tsx       │
│  ├─Username        ├─Full Name          ├─Navigation        │
│  ├─Password        ├─Phone              ├─Hero Section       │
│  └─Submit          ├─Address            └─Footer             │
│                    ├─Username                                │
│                    ├─Password (6+ chars)                    │
│                    └─Submit                                  │
│                                                              │
└────────────────────────────┬─────────────────────────────────┘
                             │
                    HTTP/JSON Requests
                    CORS Enabled
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  BACKEND (Express.js)                        │
│               http://localhost:5000/api                      │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────────── Routes ────────────────────┐        │
│  │                                                │        │
│  │  POST /auth/register    → registerHandler()   │        │
│  │  POST /auth/login       → loginHandler()      │        │
│  │                                                │        │
│  └────────────────────────────────────────────────┘        │
│                        │                                    │
│            (Validation, Hashing, DB Query)                 │
│                        │                                    │
│  ┌──────────────────── DB Query ──────────────────┐        │
│  │                                                │        │
│  │  INSERT users (...)                           │        │
│  │  INSERT login (...)                           │        │
│  │  SELECT with JOIN                             │        │
│  │  Password comparison (bcrypt)                 │        │
│  │                                                │        │
│  └────────────────────────────────────────────────┘        │
│                                                              │
└────────────────────────────┬─────────────────────────────────┘
                             │
                    TCP Connection
                    Port: 3306
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   MYSQL DATABASE                             │
│              XAMPP MySQLServer @ localhost:3306              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Database: hp_db                                            │
│  ├─ users                                                    │
│  │  ├─ user_id (PRIMARY KEY)                               │
│  │  ├─ full_name (VARCHAR)                                 │
│  │  ├─ phone (VARCHAR)                                     │
│  │  └─ address (VARCHAR)                                   │
│  │                                                          │
│  ├─ login                                                   │
│  │  ├─ login_id (PRIMARY KEY)                              │
│  │  ├─ user_id (FOREIGN KEY)                               │
│  │  ├─ username (VARCHAR UNIQUE)                           │
│  │  └─ password (VARCHAR - bcrypt hash)                   │
│  │                                                          │
│  ├─ appointments                                            │
│  │  ├─ appointment_id (PRIMARY KEY)                        │
│  │  ├─ user_id (FOREIGN KEY)                               │
│  │  ├─ department_id (FOREIGN KEY)                         │
│  │  ├─ appointment_date (DATE)                             │
│  │  ├─ additional_message (TEXT)                           │
│  │  └─ status (VARCHAR)                                    │
│  │                                                          │
│  └─ departments                                             │
│     ├─ department_id (PRIMARY KEY)                         │
│     └─ department_name (VARCHAR)                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow - রেজিস্ট্রেশন:

```
User fills form
     │
     ▼
Frontend validates
(fullName, phone, username, password)
     │
     ▼
POST /api/auth/register
{fullName, phone, address, username, password}
     │
     ▼
Backend receives
     │
     ▼
Check if username exists
(SELECT * FROM login WHERE username = ?)
     │
  ┌──┴──┐
  │No   │ Yes
  │     └─► Return 400: "Username already exists"
  ▼
Hash password with bcrypt
     │
     ▼
INSERT INTO users
(full_name, phone, address)
     │
     ▼
Get user_id (lastInsertId)
     │
     ▼
INSERT INTO login
(user_id, username, hashed_password)
     │
     ▼
Return 201: "Registration successful"
     │
     ▼
Frontend receives success
     │
     ▼
Clear form
     │
     ▼
Redirect to login page
     │
     ▼
User sees: "Registration successful! Redirecting..."
```

---

## 🔐 Data Flow - লগইন:

```
User enters credentials
     │
     ▼
Frontend validates
(username, password)
     │
     ▼
POST /api/auth/login
{username, password}
     │
     ▼
Backend receives
     │
     ▼
SELECT * FROM login l
JOIN users u ON l.user_id = u.user_id
WHERE username = ?
     │
  ┌──┴──────────┐
  │No results   │ Found
  │             │
  │ Return 401: │ ▼
  │"Invalid"    │ Compare plaintext password
  └─────────────┤ with bcrypt hash
                │
                ├─ Match ──► Return 200:
                │           {message, user}
                │
                └─ No match ──► Return 401:
                               "Invalid credentials"
     │
     ▼
Frontend receives response
     │
  ┌──┴──────────────────┐
  │Success              │Error
  │                     │
  ▼                     ▼
Save to localStorage   Show error message
     │                     │
     ▼                     ▼
Show welcome message    Clear form
     │                     
     ▼                     
Redirect to home
```

---

## 📊 Database Schema - সম্পর্ক:

```
┌─────────────────┐           ┌──────────────────┐
│     users       │           │      login       │
├─────────────────┤           ├──────────────────┤
│ user_id (PK)    │◄──FK──────│ user_id (FK)     │
│ full_name       │           │ login_id (PK)    │
│ phone           │           │ username (U)     │
│ address         │           │ password (hash)  │
└─────────────────┘           └──────────────────┘
         ▲                              │
         │                              │
         │ FK                           │ 1:N
         │                              │
         └──────────────────────────────┘


┌─────────────────┐           ┌──────────────────┐
│     users       │           │  appointments    │
├─────────────────┤           ├──────────────────┤
│ user_id (PK)    │◄──FK──────│ user_id (FK)     │
│ full_name       │           │ appointment_id   │
│ phone           │           │ department_id    │
│ address         │           │ appointment_date │
└─────────────────┘           │ additional_msg   │
                              │ status           │
                              └──────────────────┘


┌─────────────────┐           ┌──────────────────┐
│  departments    │           │  appointments    │
├─────────────────┤           ├──────────────────┤
│ dept_id (PK)    │◄──FK──────│ department_id    │
│ dept_name       │           │ appointment_id   │
└─────────────────┘           └──────────────────┘
```

---

## 🔐 নিরাপত্তা স্তর:

```
┌─────────────────────────────────────────┐
│  Frontend (React)                       │
│  - Form Validation                      │
│  - Client-side checks                   │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  HTTP/HTTPS Layer                       │
│  - CORS Enabled                         │
│  - Secure Headers                       │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Backend (Express.js)                   │
│  - Server-side Validation               │
│  - Input Sanitization                   │
└────────────┬────────────────────────────┘
             │
             ▼
┌─────────────────────────────────────────┐
│  Database Layer                         │
│  - Parameterized Queries (SQL Injection)│
│  - Bcrypt Hashing (Password Security)   │
│  - Foreign Keys (Data Integrity)        │
│  - Unique Constraints (Duplicate Check) │
└─────────────────────────────────────────┘
```

---

## 🚀 Deployment Stack:

```
LOCAL DEVELOPMENT:
├─ Node.js (Backend Runtime)
├─ React (Frontend Framework)
├─ Vite (Build Tool)
├─ Express.js (Server Framework)
├─ MySQL (Database)
└─ XAMPP (Local Development Environment)

PRODUCTION:
├─ Node.js Server (DigitalOcean/AWS/Heroku)
├─ React Static Build (Netlify/Vercel)
├─ Express.js with PM2/Forever
├─ MySQL on Cloud (RDS/Cloud SQL)
└─ HTTPS/SSL Certificate
```

---

## 📱 Response Format:

### Successful Registration:
```json
{
  "status": 201,
  "message": "রেজিস্ট্রেশন সফল হয়েছে! এখন লগইন করুন।"
}
```

### Successful Login:
```json
{
  "status": 200,
  "message": "লগইন সফল",
  "user": {
    "user_id": 1,
    "full_name": "Samir Khan",
    "username": "samir"
  }
}
```

### Error Response:
```json
{
  "status": 400,
  "message": "এই ইউজারনেম ইতিমধ্যে ব্যবহৃত"
}
```

---

## 🔄 Request/Response Cycle:

```
1. Browser Request
   ├─ POST /api/auth/register
   ├─ Headers: Content-Type: application/json
   ├─ Body: {fullName, phone, address, username, password}
   └─ Time: < 100ms

2. Server Processing
   ├─ Validate Input
   ├─ Check Duplicate Username
   ├─ Hash Password (bcrypt - ~100ms)
   ├─ Insert to Database
   └─ Time: 100-200ms

3. Browser Response
   ├─ Status: 201 Created
   ├─ Headers: Content-Type: application/json
   ├─ Body: {message, status}
   └─ Time: < 50ms

Total Response Time: ~150-250ms
```

---

এই আর্কিটেকচার **নিরাপদ**, **স্কেলেবল**, এবং **রক্ষণাবেক্ষণযোগ্য**। ✅
