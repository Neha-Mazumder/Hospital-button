# API ডকুমেন্টেশন - সম্পূর্ণ রেফারেন্স

## ভিত্তি URL
```
http://localhost:5000
```

---

## 🔐 অথেন্টিকেশন API

### 1️⃣ রেজিস্ট্রেশন
```http
POST /api/auth/register
Content-Type: application/json
```

**অনুরোধ:**
```json
{
  "fullName": "মোহাম্মদ আহমেদ",
  "phone": "01912345678",
  "address": "ঢাকা, বাংলাদেশ",
  "username": "ahmad123",
  "password": "password123"
}
```

**প্রতিক্রিয়া (সফল - 201):**
```json
{
  "message": "রেজিস্ট্রেশন সফল হয়েছে! এখন লগইন করুন।"
}
```

**প্রতিক্রিয়া (ত্রুটি - 400):**
```json
{
  "message": "এই ইউজারনেম ইতিমধ্যে ব্যবহৃত"
}
```

**ভ্যালিডেশন:**
- সমস্ত ফিল্ড বাধ্যতামূলক
- পাসওয়ার্ড কমপক্ষে 6 অক্ষর
- ইউজারনেম অনন্য হওয়া আবশ্যক
- ফোন নম্বর 11 অক্ষর হওয়া উচিত

**ডাটাবেস সংরক্ষণ:**
```
✅ users টেবিল:
   - full_name = "মোহাম্মদ আহমেদ"
   - phone = "01912345678"
   - address = "ঢাকা, বাংলাদেশ"

✅ login টেবিল:
   - username = "ahmad123"
   - password = "password123" (প্লেইনটেক্সট)
   - user_id = (নতুন ব্যবহারকারীর ID)
```

---

### 2️⃣ লগইন
```http
POST /api/auth/login
Content-Type: application/json
```

**অনুরোধ:**
```json
{
  "username": "ahmad123",
  "password": "password123"
}
```

**প্রতিক্রিয়া (সফল - 200):**
```json
{
  "message": "লগইন সফল",
  "user": {
    "user_id": 1,
    "full_name": "মোহাম্মদ আহমেদ",
    "username": "ahmad123",
    "phone": "01912345678"
  }
}
```

**প্রতিক্রিয়া (ত্রুটি - 401):**
```json
{
  "message": "ভুল ইউজারনেম বা পাসওয়ার্ড"
}
```

**বৈশিষ্ট্য:**
- প্লেইনটেক্সট পাসওয়ার্ড তুলনা
- সম্পূর্ণ ব্যবহারকারী বিবরণ ফেরত দিন
- localStorage এ সংরক্ষণ করা হয় ফ্রন্টএন্ডে

---

## 📅 অ্যাপয়েন্টমেন্ট API

### 3️⃣ সমস্ত ডিপার্টমেন্ট পান
```http
GET /api/appointments/departments
```

**প্রতিক্রিয়া (200):**
```json
[
  {
    "department_id": 1,
    "department_name": "Cardiology"
  },
  {
    "department_id": 2,
    "department_name": "Neurology"
  },
  {
    "department_id": 3,
    "department_name": "Orthopedics"
  },
  {
    "department_id": 4,
    "department_name": "Dermatology"
  },
  {
    "department_id": 5,
    "department_name": "Pediatrics"
  }
]
```

**ব্যবহার:**
- ড্রপডাউন মেনু পূরণ করতে
- কোনও পাথ প্যারামিটার প্রয়োজন নেই

---

### 4️⃣ নতুন অ্যাপয়েন্টমেন্ট বুক করুন
```http
POST /api/appointments/book
Content-Type: application/json
```

**অনুরোধ:**
```json
{
  "user_id": 1,
  "department_id": 1,
  "appointment_date": "2024-12-20",
  "additional_message": "আমার হৃদরোগের ইতিহাস রয়েছে, দয়া করে যত্ন সহকারে দেখুন"
}
```

**প্রতিক্রিয়া (সফল - 201):**
```json
{
  "message": "অ্যাপয়েন্টমেন্ট সফলভাবে বুক করা হয়েছে",
  "appointmentId": 15
}
```

**প্রতিক্রিয়া (ত্রুটি - 400):**
```json
{
  "message": "সমস্ত প্রয়োজনীয় ফিল্ড প্রদান করুন"
}
```

**ভ্যালিডেশন:**
- user_id বাধ্যতামূলক
- department_id বাধ্যতামূলক
- appointment_date বাধ্যতামূলক (YYYY-MM-DD ফর্ম্যাট)
- additional_message ঐচ্ছিক

**স্বয়ংক্রিয় মান:**
- status = "Pending"
- created_at = বর্তমান সময়

**ডাটাবেস সংরক্ষণ:**
```
INSERT INTO appointments 
(user_id, department_id, appointment_date, additional_message, status, created_at)
VALUES (1, 1, '2024-12-20', 'বার্তা', 'Pending', NOW())
```

---

### 5️⃣ ব্যবহারকারীর অ্যাপয়েন্টমেন্ট পান
```http
GET /api/appointments/user/:user_id
```

**পাথ প্যারামিটার:**
- `user_id` - পূর্ণসংখ্যা (প্রয়োজনীয়)

**উদাহরণ:**
```
GET /api/appointments/user/1
```

**প্রতিক্রিয়া (200):**
```json
[
  {
    "appointment_id": 15,
    "user_id": 1,
    "department_id": 1,
    "department_name": "Cardiology",
    "appointment_date": "2024-12-20",
    "additional_message": "আমার হৃদরোগের ইতিহাস রয়েছে",
    "status": "Pending",
    "created_at": "2024-12-15T10:30:00.000Z"
  },
  {
    "appointment_id": 16,
    "user_id": 1,
    "department_id": 2,
    "department_name": "Neurology",
    "appointment_date": "2024-12-25",
    "additional_message": "মাথা ব্যথার সমস্যা",
    "status": "Cancelled",
    "created_at": "2024-12-16T14:15:00.000Z"
  }
]
```

**বৈশিষ্ট্য:**
- সর্বশেষ প্রথম সাজানো (ORDER BY created_at DESC)
- বিভাগের নাম অন্তর্ভুক্ত
- সমস্ত স্ট্যাটাস অন্তর্ভুক্ত (Pending, Cancelled, Completed)

---

### 6️⃣ অ্যাপয়েন্টমেন্ট বাতিল করুন
```http
PUT /api/appointments/cancel/:appointment_id
Content-Type: application/json
```

**পাথ প্যারামিটার:**
- `appointment_id` - পূর্ণসংখ্যা (প্রয়োজনীয়)

**উদাহরণ:**
```
PUT /api/appointments/cancel/15
```

**অনুরোধ বডি:**
```json
{}
```

**প্রতিক্রিয়া (সফল - 200):**
```json
{
  "message": "অ্যাপয়েন্টমেন্ট সফলভাবে বাতিল করা হয়েছে"
}
```

**প্রতিক্রিয়া (ত্রুটি - 404):**
```json
{
  "message": "অ্যাপয়েন্টমেন্ট পাওয়া যায়নি"
}
```

**ব্যবসায়িক যুক্তি:**
- শুধুমাত্র "Pending" স্ট্যাটাসের অ্যাপয়েন্টমেন্ট বাতিল করা যায়
- স্ট্যাটাস "Cancelled" এ পরিবর্তিত হয়
- সম্পূর্ণ এবং অন্যান্য স্ট্যাটাস বাতিল করা যায় না

---

## 🔄 সম্পূর্ণ ফ্লো উদাহরণ

### ধাপ 1: রেজিস্টার
```bash
curl -X POST http://localhost:5000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "fullName": "রহিম আলী",
    "phone": "01987654321",
    "address": "চট্টগ্রাম",
    "username": "rahim99",
    "password": "secure123"
  }'
```

### ধাপ 2: লগইন
```bash
curl -X POST http://localhost:5000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "rahim99",
    "password": "secure123"
  }'

# প্রতিক্রিয়া থেকে user_id নিন (উদাহরণে: 2)
```

### ধাপ 3: ডিপার্টমেন্ট পান
```bash
curl -X GET http://localhost:5000/api/appointments/departments
```

### ধাপ 4: অ্যাপয়েন্টমেন্ট বুক করুন
```bash
curl -X POST http://localhost:5000/api/appointments/book \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 2,
    "department_id": 1,
    "appointment_date": "2024-12-22",
    "additional_message": "প্রাথমিক পরামর্শ"
  }'
```

### ধাপ 5: আপনার অ্যাপয়েন্টমেন্ট পান
```bash
curl -X GET http://localhost:5000/api/appointments/user/2
```

### ধাপ 6: অ্যাপয়েন্টমেন্ট বাতিল করুন
```bash
curl -X PUT http://localhost:5000/api/appointments/cancel/1 \
  -H "Content-Type: application/json" \
  -d '{}'
```

---

## ⚠️ সাধারণ ত্রুটি এবং সমাধান

### 400 Bad Request
**কারণ:** অনুপস্থিত বা অবৈধ প্যারামিটার
```json
{
  "message": "সকল ফিল্ড আবশ্যক"
}
```
**সমাধান:** অনুরোধ বডি যাচাই করুন এবং সমস্ত প্রয়োজনীয় ফিল্ড অন্তর্ভুক্ত করুন

### 401 Unauthorized
**কারণ:** ভুল ক্রেডেনশিয়াল
```json
{
  "message": "ভুল ইউজারনেম বা পাসওয়ার্ড"
}
```
**সমাধান:** ইউজারনেম এবং পাসওয়ার্ড যাচাই করুন

### 404 Not Found
**কারণ:** রিসোর্স পাওয়া যায়নি
```json
{
  "message": "অ্যাপয়েন্টমেন্ট পাওয়া যায়নি"
}
```
**সমাধান:** সঠিক ID ব্যবহার করুন

### 500 Internal Server Error
**কারণ:** ডাটাবেস সংযোগ সমস্যা
```json
{
  "message": "ত্রুটি বার্তা"
}
```
**সমাধান:** 
1. MySQL চলছে কিনা যাচাই করুন
2. ডাটাবেস hp_db বিদ্যমান কিনা দেখুন
3. ব্যাকএন্ড সার্ভার পুনরায় চালু করুন

---

## 📊ডাটাবেস ক্ষেত্র রেফারেন্স

### users টেবিল
| ক্ষেত্র | টাইপ | বর্ণনা |
|--------|------|--------|
| user_id | INT | প্রাথমিক চাবি, স্বয়ংক্রিয় বৃদ্ধি |
| full_name | VARCHAR(100) | ব্যবহারকারীর সম্পূর্ণ নাম |
| phone | VARCHAR(11) | ফোন নম্বর |
| address | VARCHAR(255) | সম্পূর্ণ ঠিকানা |

### login টেবিল
| ক্ষেত্র | টাইপ | বর্ণনা |
|--------|------|--------|
| login_id | INT | প্রাথমিক চাবি |
| user_id | INT | বিদেশী চাবি (users.user_id) |
| username | VARCHAR(50) | অনন্য ইউজারনেম |
| password | VARCHAR(255) | প্লেইনটেক্সট পাসওয়ার্ড |

### appointments টেবিল
| ক্ষেত্র | টাইপ | বর্ণনা |
|--------|------|--------|
| appointment_id | INT | প্রাথমিক চাবি |
| user_id | INT | বিদেশী চাবি (users.user_id) |
| department_id | INT | বিদেশী চাবি (departments.department_id) |
| appointment_date | DATE | অ্যাপয়েন্টমেন্টের তারিখ (YYYY-MM-DD) |
| additional_message | TEXT | অতিরিক্ত নোট/বার্তা |
| status | VARCHAR(50) | Pending / Cancelled / Completed |
| created_at | TIMESTAMP | রেকর্ড তৈরির সময় |

### departments টেবিল
| ক্ষেত্র | টাইপ | বর্ণনা |
|--------|------|--------|
| department_id | INT | প্রাথমিক চাবি |
| department_name | VARCHAR(100) | বিভাগের নাম |

---

## 🔗 সংযোগ তথ্য

**সার্ভার:**
- ঠিকানা: localhost
- পোর্ট: 3306
- ডাটাবেস: hp_db

**ব্যবহারকারী:**
- নাম: root
- পাসওয়ার্ড: (খালি)

---

## ✅ সমর্থিত স্ট্যাটাস মান

```
অ্যাপয়েন্টমেন্ট স্ট্যাটাস:
- "Pending" → অপেক্ষমাণ (নতুন অ্যাপয়েন্টমেন্ট)
- "Completed" → সম্পন্ন (চিকিৎসা শেষ)
- "Cancelled" → বাতিল করা হয়েছে (ব্যবহারকারী দ্বারা)
```

---

## 📝 নোট

1. সমস্ত পাসওয়ার্ড প্লেইনটেক্সটে সংরক্ষিত (ডেভেলপমেন্ট)
2. CORS সক্ষম (সমস্ত অরিজিন থেকে অ্যাক্সেস)
3. প্যারামিটারাইজড কোয়েরি ব্যবহার করা হয় (SQL ইনজেকশন সুরক্ষা)
4. সমস্ত তারিখ UTC/GMT তে
5. 404 ত্রুটি যখন রিসোর্স পাওয়া যায় না

---

**API সংস্করণ:** 1.0  
**আপডেট:** 2024-12-16  
**স্ট্যাটাস:** প্রযোজনীয়  
